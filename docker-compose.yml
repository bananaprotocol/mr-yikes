version: '3'

services:
        bot:
                build: .
                restart: always
                environment:
                        - TOKEN=
                        - TARGET_ID=
                        - TARGET_ROLE=
                        - EMOTE=

